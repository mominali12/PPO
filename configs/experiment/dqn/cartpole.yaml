# @package _global_
defaults:
  - override /algorithm: dqn
  - override /environment: cartpole
  - override /logger: []
  - _self_

trainer:
  total_frames: 100_000
  seed: 42

algorithm:
  frames_per_batch: 200
  init_random_frames: 1_000
  replay_buffer:
    capacity: 10_000
    batch_size: 64
  eps_annealing_frames: 10_000
